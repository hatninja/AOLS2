--Dialogue
feature_autocolor		= true  --Allows recoloring of text based on contents.
feature_ccmarkdown		= true  --Enable special support for CC markdown. Will behave regularly if disabled.

--Server
feature_freepick		= true	--Allow any character to be chosen. This will also boot the player into lobby.
feature_dynamicrooms 	= true	--Allow creation of rooms.

--Protocol
feature_fastslowload	= true	--Allow AO1's loading 1.0 to be queue everything at once. If disabled, you get slower but normal behaviour.
